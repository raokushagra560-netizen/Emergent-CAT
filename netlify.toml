[build]
  base = "frontend"
  command = "npm install ajv@^8.12.0 --legacy-peer-deps && npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "16"
  NPM_FLAGS = "--legacy-peer-deps"
