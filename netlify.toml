[build]
  base = "frontend"
  command = "npm install ajv@^8.12.0 schema-utils@^4.0.0 --legacy-peer-deps && npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"
